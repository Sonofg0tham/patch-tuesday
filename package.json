{
  "name": "patch-tuesday",
  "private": true,
  "version": "0.0.1",
  "description": "Turn-based incident response tactics game. Phase -1 spike.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.182.0"
  },
  "devDependencies": {
    "@types/three": "^0.182.0",
    "typescript": "^5.8.0",
    "vite": "^7.1.0"
  }
}
